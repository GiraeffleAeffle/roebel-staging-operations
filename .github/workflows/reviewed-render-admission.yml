name: reviewed-render-admission

on:
  push:
    branches:
      - main
  pull_request_target:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review

permissions:
  contents: read

concurrency:
  group: reviewed-render-admission-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  reviewed-render-admission:
    name: reviewed-render-admission
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    env:
      PYTHONDONTWRITEBYTECODE: "1"
    steps:
      - name: Check out protected base
        if: github.event_name == 'pull_request_target'
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: base
          persist-credentials: false
          # Existing activation tests bind the exact historical b790 receipt
          # to its protected Git blob, so the trusted base needs that history.
          fetch-depth: 0

      - name: Check out untrusted candidate as data
        if: github.event_name == 'pull_request_target'
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.sha }}
          path: candidate
          persist-credentials: false
          fetch-depth: 1

      - name: Verify pull request with protected base policy
        if: github.event_name == 'pull_request_target'
        run: |
          python3 -m unittest -v base/scripts/test_staging_participant_gateway_policy.py base/scripts/test_staging_participant_flux_bootstrap.py base/scripts/test_activate_staging_participant_gateway.py base/scripts/test_materialize_staging_participant_gateway_secrets.py base/scripts/test_run_staging_participant_gateway_live.py base/scripts/test_reset_staging_relay_fixtures.py base/scripts/test_promote_staging_workbench_image.py base/scripts/test_tracer_data_plane_policy.py base/scripts/test_verify_reviewed_render.py base/scripts/test_workbench_baseline_handover.py base/scripts/test_workbench_baseline_recovery.py base/tests/test_stadtstack_case_runtime_contract.py base/tests/test_stadtstack-case-recovery-composition-contract.py base/tests/test_stadtstack_case_image_resource_inventory_contract.py
          python3 base/scripts/verify-reviewed-render.py --root candidate --base-root base
          python3 base/scripts/verify-stadtstack-case-recovery-composition-contract.py --root candidate
          python3 base/scripts/verify-stadtstack-case-image-resource-inventory-contract.py --root candidate

      - name: Check out protected main
        if: github.event_name == 'push'
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.sha }}
          path: candidate
          persist-credentials: false
          # The protected-main suite verifies the same historical binding.
          fetch-depth: 0

      - name: Verify protected main
        if: github.event_name == 'push'
        run: |
          python3 -m unittest -v candidate/scripts/test_staging_participant_gateway_policy.py candidate/scripts/test_staging_participant_flux_bootstrap.py candidate/scripts/test_activate_staging_participant_gateway.py candidate/scripts/test_materialize_staging_participant_gateway_secrets.py candidate/scripts/test_run_staging_participant_gateway_live.py candidate/scripts/test_reset_staging_relay_fixtures.py candidate/scripts/test_promote_staging_workbench_image.py candidate/scripts/test_tracer_data_plane_policy.py candidate/scripts/test_verify_reviewed_render.py candidate/scripts/test_workbench_baseline_handover.py candidate/scripts/test_workbench_baseline_recovery.py candidate/tests/test_stadtstack_case_runtime_contract.py candidate/tests/test_stadtstack-case-recovery-composition-contract.py candidate/tests/test_stadtstack_case_image_resource_inventory_contract.py
          python3 candidate/scripts/verify-reviewed-render.py --root candidate
          python3 candidate/scripts/verify-stadtstack-case-recovery-composition-contract.py --root candidate
          python3 candidate/scripts/verify-stadtstack-case-image-resource-inventory-contract.py --root candidate
